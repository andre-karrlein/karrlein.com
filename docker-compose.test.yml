version: '2'
services:
    karrlein-nginx:
        labels:
            - "traefik.backend=karrlein.com"
            - "traefik.docker.network=web"
            - "traefik.frontend.rule=Host:dev.karrlein.com"
            - "traefik.enable=true"

